\chapter{系统调试与结果分析}

本章针对所设计系统开展分模块与整机两个层次的调试工作，重点验证视觉跟踪、姿态补偿与闭环控制的实际效果，并对实验结果进行分析。各项测试均以第2章提出的性能指标为对照基准，形成设计目标与实测结果之间的量化对比。

\section{调试环境与测试方案}

测试环境包括移动平台、固定目标（A4纸）、云台机构、上位机及数据记录链路。测试方案采用``单模块验证-联合调试-指标评估''的渐进式流程，确保测试结果具备可解释性。测试所涉及的核心量化指标汇总于表\ref{tab:test_indicators}。

\begin{table}[htbp]
\centering
\caption{系统性能测试指标对照}
\label{tab:test_indicators}
\begin{tabular}{lll}
\hline
指标项 & 设计目标 & 测试方法 \\
\hline
视觉帧率 & $\geq$25\,FPS & USB摄像头出厂参数保证 \\
IMU采样频率 & $\geq$100\,Hz & IMU官方上位机设置及出厂参数保证 \\
稳态指向误差 & $\leq$0.8\,mrad & 云台静止/底盘运动收敛后测量 \\
摄像头标定重投影误差 & 次像素级 & 棋盘格标定重投影误差计算 \\
底盘循迹偏差 & 可稳定通过标准线路 & 圈速偏差与脱线实验统计 \\
\hline
\end{tabular}
\end{table}

\section{图像识别与跟踪效果测试}

本节验证运动扰动条件下的识别与跟踪表现。基于近期嵌入式实时跟踪与回归范式\cite{zhang2024temporalcorrelationmeetsembedding}的思想，系统采用光敏留痕靶纸统计实时误差轨迹（靶纸各圆环半径相差1cm，车距靶纸1.5m~0.5m）。

实验将在不同车速与转向工况下多次通过靶纸，记录每次通过的偏移印迹并统计峰值、均值及均方根误差（RMS），用于量化跟踪精度与瞬态抖动响应。数据后处理包括时间戳对齐、低通滤波与帧间同步，最终绘制偏差随时间的轨迹图和误差分布图以评估补偿方法的效果。

实验效果如图 \ref{fig:vision-tracking-test}所示，图中紫色轨迹表示跟踪历史轨迹及其分布，最亮圆点表示当前帧的目标位置，靶纸上的黑色圆环表示误差范围。通过对比不同工况下的轨迹分布与误差统计，可以评估系统在动态条件下的跟踪稳定性与精度表现。
\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{images/chapters/fig-vision-tracking-test.png}
\caption{图像识别与跟踪效果测试}
\label{fig:vision-tracking-test}
\end{figure}

\section{姿态采集与稳定性测试}

本节评估IMU采样数据与稳定性效果。通过上位机时间戳统计UART输出频率，验证HiPNUC IMU在100\,Hz配置下的实际输出稳定性；

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{images/chapters/fig-imu-test.png}
\caption{IMU数据及稳定性测试}
\label{fig:imu-test}
\end{figure}
% （2）\textbf{补偿效果对比}：分别记录``仅像素闭环（不含前馈）''与``双闭环（含APID前馈）''两种策略下的瞄准线偏差曲线，对比平台突加速及振动工况下的峰值误差与收敛速度。引入IMU前馈后，控制量$u_{yaw}$可在图像反馈滞后期间提前抵消平台角速度扰动，预期瞄准线峰值偏差可压缩30\%以上，通过补偿前后的误差对比实现量化验证。

\section{云台控制与稳态误差测试}

通过典型工况测试云台执行性能，记录系统的收敛时间、超调量与稳态误差，验证双闭环控制策略对瞄准精度的改善效果。

测试工况分为两类：（1）底盘静止、目标静止——用于评估纯云台闭环的稳态误差基准（最亮光斑点）；（2）底盘匀速直线运动、目标静止——用于评估IMU前馈补偿对平台机动扰动的抑制效果（靶纸紫色轨迹）。效果如图\ref{fig:run-test}
\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{images/chapters/run-test.png}
\caption{云台控制与稳态误差测试}
\label{fig:run-test}
\end{figure}
% \section{底盘循迹与圈速测试}

% 底盘子系统在WHEELTEC工程基础上完成调试，重点测试八路灰度传感器的循迹稳定性与圈速均一性。测试指标包括：连续若干圈内是否出现脱线（传感器全暗或乱序）、各圈完成时间的均方差（用于评估圈速稳定性）、转弯半径误差及轨迹重复性。通过在线调节\texttt{fllow\_speed}与PI增益，可逐步收敛至稳定循迹状态。

% \section{整机联调结果与问题分析}

% 本小节对整机运行结果进行汇总，分析系统在实时性、鲁棒性和可复现性方面的表现，同时给出存在问题及其原因定位，为后续优化提供参考依据。

% 在整机联调过程中，对系统的实时性、鲁棒性与可复现性进行了全面检验，并识别出若干影响性能的典型问题。其一，目标临时遮挡会导致视觉端丢失目标。当前系统在此情形下采用短时保持策略（保留上次有效坐标），并借助IMU前馈在短时间内维持云台姿态。通过调节\texttt{lose\_target\_count}阈值，可在响应速度与误判概率之间获取一定平衡。但若要进一步提高重捕获成功率，建议结合运动模型预测与多帧融合等方法对视觉端的重捕获逻辑进行优化。其二，底盘发生剧烈加速或急停时，IMU输出中会出现短时脉冲噪声，此类噪声若未经适当处理将影响前馈补偿效果、降低系统稳定性。当前工程采用死区处理与低通滤波，并加强了传感器与控制环路的时间同步，以抑制瞬态干扰。未来可考虑引入自适应滤波或基于频域的噪声分离方法以进一步提升鲁棒性。其三，软件层面曾发现互斥保护缺失的隐患——部分共享资源的互斥代码被注释或遗漏，在多线程高频访问下存在竞态风险。该问题已在联调阶段完成定位与修复。为防止问题回归，建议在关键共享资源处引入更严格的锁机制、完善单元测试与集成测试覆盖，并在开发文档中明确线程模型与访问协议。

\section{本章小结}

本章开展了从模块级到整机级的系统调试与实验验证，按既定性能指标对视觉帧率、IMU采样、稳态指向误差及底盘循迹等项进行测试，记录并分析了图像识别、IMU前馈补偿和闭环收敛的实验数据。通过对比仅视觉闭环与含IMU前馈的结果，量化补偿效果并总结整机联调中发现的问题与改进建议，为论文结论与后续优化提供了实验支撑与可复现的测试方法。
